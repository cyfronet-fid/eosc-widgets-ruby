<main>

  <section class="card">
    <div class="card-body">
      <p class="card-text">Welcome in EOSC Widgets - application that aggregates
        all shared features for EOSC.</p>
      <input type="text" class="form-control" data-color-target="color">
      <button class="btn btn-primary" data-action="click->color#changeColor">Change Colors</button>
    </div>
  </section>
</main>

