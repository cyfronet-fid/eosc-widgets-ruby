<h2>New Service</h2>

<% if @service.errors.any? %>
  <div class="alert alert-danger">
    <ul class="mb-0">
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render_partial 'services/form' %>
